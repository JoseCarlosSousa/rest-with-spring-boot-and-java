server:
  port: 8888
  
spring:
  application:
    name: rest-with-spring-boot-and-java
  datasource:


  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    open-in-view: false
    
logging:
  level:
    root: INFO
    #pt.seixal.carlos: INFO
    
spring-doc:
  paths-to-scan: /api/**/v1/**
  swagger-ui:
    use-root-path: true
